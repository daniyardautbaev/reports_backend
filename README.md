Reports Backend — Django + JWT
Минималистичный backend-сервис для управления отчётами с аутентификацией JWT и ролевой моделью пользователей (admin, staff).

Запуск проекта
1. Клонирование и установка зависимостей

```
git clone https://github.com/daniyardautbaev/reports_backend
cd reports_backend

python3 -m venv venv
source venv/bin/activate     # Windows: venv\Scripts\activate

pip install django djangorestframework djangorestframework-simplejwt

```
2. Применение миграций
```

python3 manage.py makemigrations
python3 manage.py migrate

```

3. Создание администратора

```
python3 manage.py runserver

```

API доступно по адресу:
👉 http://127.0.0.1:8000/

Аутентификация (JWT)
Используется библиотека SimpleJWT.
Доступные эндпоинты:



POST /auth/register
Регистрация нового пользователя (роль по умолчанию — staff).
Пример запроса:

```
{
  "username": "user1",
  "password": "password123",
  "email": "user1@example.com"
}

```

POST /auth/login
Возвращает JWT-токены и информацию о пользователе.
Пример ответа:

```
{
  "refresh": "...",
  "access": "...",
  "user": {
    "id": 1,
    "username": "user1",
    "role": "staff"
  }
}


```

GET /auth/me
Возвращает профиль текущего пользователя.

Заголовок:

```
Authorization: Bearer <access_token>

```

Работа с отчётами
🔹 POST /reports
Создание отчёта.
Сохраняются:
category
message (обязательное поле, не может быть пустым)
автор (берётся из JWT)
время создания


GET /reports
Доступ зависит от роли пользователя:
Роль	Что видит
admin	все отчёты
staff	только свои

Структура проекта

```
project/
│
├── config/                # настройки Django
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
│
├── core/                  # основная логика приложения
│   ├── models.py          # модели User и Report
│   ├── serializers.py     # сериализация данных
│   ├── views.py           # обработчики запросов
│   ├── urls.py            # маршруты API
│   └── admin.py           # админка
│
├── manage.py
└── README.md



```

Используемые технологии
Django
Django REST Framework
SimpleJWT
SQLite / PostgreSQL
Статус проекта
Проект полностью реализует требования:
✔ JWT-аутентификация
✔ Хранение паролей в хэшированном виде
✔ Роли пользователей: admin, staff
✔ Создание и просмотр отчётов
✔ Разграничение прав доступа
✔ Работа с базой данных (users, reports)
